{
  "name": "lifecompass-referral-lambda",
  "version": "1.0.0",
  "description": "LifeCompass Referral System Lambda Function",
  "main": "referral-sync-handler.js",
  "dependencies": {
    "aws-sdk": "^2.1692.0"
  },
  "devDependencies": {},
  "scripts": {
    "build": "npm install --production",
    "package": "zip -r ../referral-lambda-deployment.zip .",
    "deploy": "aws lambda update-function-code --function-name LifeCompass-ReferralSyncHandler --zip-file fileb://../referral-lambda-deployment.zip"
  }
}